# Where the FastAPI backend is reachable from the browser.
# In dev you can leave this empty and let Vite proxy /api -> backend.
# In production set it to your deployed API origin, e.g. https://api.example.com
VITE_API_URL=
